# Example environment variables for Feed Service

# PostgreSQL connection string (Supabase or local Postgres)
# schema=feed 已在 config.yaml 中设置，pgxpoolx 会自动将 search_path 调整为 feed,public
# Supabase 建议使用 sslmode=require；若本地纯 TCP 需关闭，可在个人 .env 中改为 sslmode=disable
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@localhost:54321/feed?sslmode=require
